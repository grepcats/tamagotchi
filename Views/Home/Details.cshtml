<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>@Model.GetName()</title>
  </head>
  <body>
    <div class="container">
      <div class="well">
        <h1>Hi, my name is @Model.GetName()!!</h1>
        <h4>My level of hunger is @Model.GetFoodLevel()</h4>
        <h4>My level of Sleep is @Model.GetSleepLevel()</h4>
        <h4>My level of happiness is @Model.GetAttentionLevel()</h4>
      </div>

      <form method="post" action="/feed-pet/@Model.GetId()">
        <button type="submit" class="btn btn-default">Feed me</button>
      </form>
      <form action="/sleep/@Model.GetId()" method="post">
        <button type="submit" name="button" class="btn btn-default">Sleep</button>
      </form>
      <form method="post" action="/attention/@Model.GetId()">
        <button type="submit" class="btn btn-default">Give Attention</button>
      </form>
      <form method="post" action="/wait/@Model.GetId()">
        <button type="submit" class="btn btn-default">Wait</button>
      </form>
      <h4><a href="/">Return to Tamagotchi</a></h4>
    </div>

    </body>
</html>
